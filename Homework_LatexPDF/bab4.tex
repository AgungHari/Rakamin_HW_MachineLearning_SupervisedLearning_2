\newpage

\section{Pengujian dan analisis}
Pada bab ini, akan dijelaskan mengenai hasil pengujian dan pembahasan dari penelitian yang telah diuraikan pada metodologi. Selain itu, akan dipaparkan juga mengenai skenario pengujian yang dilakukan untuk mengevaluasi performa sistem secara keseluruhan. Pengujian ini dilakukan dengan tujuan untuk memastikan bahwa sistem yang dirancang mampu berfungsi dengan baik dalam memprediksi apakah pelanggan akan churn atau tidak berdasarkan data yang telah diproses pada tahap sebelumnya.


\subsection{Pengujian Sistem}
Pengujian sistem dilakukan dengan menggunakan dataset yang telah diolah sebelumnya dan dibagi menjadi data latih dan data uji. Data latih digunakan untuk melatih model Logistic Regression, sedangkan data uji digunakan untuk menguji performa model yang telah dilatih. Adapun skenario pengujian yang dilakukan adalah sebagai berikut:
\begin{enumerate}
    \item \textbf{Pengujian Model Logistic Regression:} Model Logistic Regression dilatih menggunakan data latih yang telah disiapkan. Proses pelatihan ini melibatkan penyesuaian parameter model untuk memaksimalkan akurasi prediksi terhadap data latih.
    \item \textbf{Perbandingan dengan Model Lain:} Jika ada, model lain yang lebih kompleks seperti Decision Tree, Random Forest atau XGBoost juga diuji untuk membandingkan performa dengan model Logistic Regression. Hal ini dilakukan untuk melihat apakah model yang lebih kompleks memberikan hasil yang lebih baik dalam memprediksi churn pelanggan.
\end{enumerate}

\subsection{Pengujian model Logistic Regression}
Pengujian model Logistic Regression dilakukan dengan menggunakan data uji yang telah disiapkan. Model ini dilatih pada data latih dan kemudian diuji pada data uji untuk mengevaluasi performanya. Hasil dari pengujian ini akan memberikan gambaran tentang seberapa baik model dalam memprediksi churn pelanggan.

Setelah dilakukan pengujian model Logistic Regression, diperoleh hasil confusion matrix yang merepresentasikan performa model dalam mengklasifikasikan data uji. Confusion matrix ini menunjukkan jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Dari confusion matrix, dapat dihitung beberapa metrik evaluasi seperti akurasi, presisi, recall, dan F1-score.

\newpage
\subsubsection{Hasil Logistic Regression tanpa hyperparameter tuning}

Berikut adalah hasil confusion matrix dari model Logistic Regression yang telah diuji pada data uji, Gambar \ref{fig:confusion_matrix_logistic_regression} menunjukkan confusion matrix yang diperoleh dari model Logistic Regression tanpa melakukan hyperparameter tuning. Sehingga baseline model Logistic Regression ini akan menjadi acuan untuk perbandingan dengan model-model lainnya yang akan diuji pada tahap selanjutnya. Namun kita tetap perlu menambahkan beberapa setting manual karena pada bab sebelumnya kita menemukan imbalance data pada dataset yang digunakan. Oleh karena itu, kita perlu mengatur beberapa parameter pada model Logistic Regression untuk mengatasi masalah tersebut. Berikut adalah parameter yang digunakan pada model Logistic Regression:

\begin{itemize}
    \item \textbf{max\_iter=1000}: Menentukan jumlah iterasi maksimum pada proses optimisasi. Nilai ini dipilih agar proses pelatihan memiliki cukup iterasi untuk mencapai konvergensi.
    \item \textbf{class\_weight='balanced'}: Mengatur bobot kelas secara otomatis berdasarkan proporsi jumlah sampel pada setiap kelas. Parameter ini digunakan untuk mengatasi permasalahan ketidakseimbangan data (\textit{imbalanced dataset}) dengan memberikan bobot lebih besar pada kelas minoritas (Churn).
    \item \textbf{solver='liblinear'}: Algoritma optimisasi yang digunakan. \textit{Liblinear} cocok untuk dataset berukuran kecil hingga menengah dan mendukung regularisasi L1 maupun L2.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{gambar/logisticregressionfix.png}
    \caption{Confusion Matrix Model Logistic Regression}
    \label{fig:confusion_matrix_logistic_regression}
\end{figure}

Dari gambar \ref{fig:confusion_matrix_logistic_regression} kita bisa identifikasi performa model Logistic Regression dalam mengklasifikasikan data uji. Confusion matrix ini memberikan informasi tentang jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Detailnya adalah sebagai berikut:

\begin{itemize}
    \item True Negative (TN): 723 (Pelanggan yang benar-benar tidak churn dan diprediksi tidak churn)
    \item False Positive (FP): 310 (Pelanggan yang tidak churn, tetapi diprediksi churn)
    \item False Negative (FN): 78 (Pelanggan yang churn, tetapi diprediksi tidak churn)
    \item True Positive (TP): 296 (Pelanggan yang benar-benar churn dan diprediksi churn)
\end{itemize}

% masukan semua hasil prediksi kedalam rumus pencarian accuracy, recall, precision
Setelah mendapatkan confusion matrix, kita dapat menghitung beberapa metrik evaluasi untuk model Logistic Regression. Metrik yang umum digunakan adalah akurasi, presisi, recall, dan F1-score. 

\subsubsection*{Akurasi}

Akurasi adalah proporsi prediksi yang benar dari total prediksi yang dilakukan oleh model. Berikut adalah perhitungan akurasi model Logistic Regression:
\begin{equation}
    \text{Akurasi} = \frac{TP + TN}{TP + TN + FP + FN} = \frac{296 + 723}{296 + 723 + 310 + 78} = \frac{1019}{1407} \approx 0.724
\end{equation}
Akurasi model Logistic Regression adalah sekitar 72.4\%. Ini menunjukkan bahwa model ini mampu memprediksi dengan benar sekitar 72.4\% dari total data uji yang diberikan.

\subsubsection*{Precision}

Precision adalah proporsi prediksi positif yang benar dari total prediksi positif yang dilakukan oleh model. Berikut adalah perhitungan presisi model Logistic Regression:
\begin{equation}
    \text{Presisi} = \frac{TP}{TP + FP} = \frac{296}{296 + 310} = \frac{296}{606} \approx 0.488
\end{equation}
Presisi model Logistic Regression adalah sekitar 48.8\%. Ini menunjukkan bahwa dari semua pelanggan yang diprediksi churn, hanya sekitar 48.8\% yang benar-benar churn. Hal ini mengindikasikan bahwa model ini masih menghasilkan cukup banyak false positive, yaitu pelanggan yang diprediksi churn padahal sebenarnya tidak.


\subsubsection*{Recall}

Recall adalah proporsi prediksi positif yang benar dari total aktual positif. Berikut adalah perhitungan recall model Logistic Regression:
\begin{equation}
    \text{Recall} = \frac{TP}{TP + FN} = \frac{296}{296 + 78} = \frac{296}{374} \approx 0.791
\end{equation}
Recall model Logistic Regression adalah sekitar 79.1\%. Ini menunjukkan bahwa model ini mampu menangkap sekitar 79.1\% dari pelanggan yang sebenarnya churn, namun masih ada 20.9\% pelanggan churn yang tidak terdeteksi oleh model.

\subsubsection*{F1-score}

F1-score adalah rata-rata harmonis dari presisi dan recall. F1-score memberikan gambaran yang lebih baik tentang keseimbangan antara presisi dan recall. Berikut adalah perhitungan F1-score model Logistic Regression:
\begin{equation}
    \text{F1-score} = 2 \cdot \frac{\text{Presisi} \cdot \text{Recall}}{\text{Presisi} + \text{Recall}} = 2 \cdot \frac{0.488 \cdot 0.791}{0.488 + 0.791} \approx 0.604
\end{equation}
F1-score model Logistic Regression adalah sekitar 60.4\%. Ini menunjukkan bahwa model ini memiliki keseimbangan yang cukup baik antara presisi dan recall, meskipun masih ada ruang untuk perbaikan, terutama dalam hal menangkap pelanggan yang churn.

\subsubsection*{ROC-AUC}
ROC-AUC (Receiver Operating Characteristic - Area Under the Curve) adalah metrik yang digunakan untuk mengukur kemampuan model dalam membedakan antara kelas positif dan negatif. Adapun nilai ROC-AUC yang didapatkan dari model Logistic Regression adalah 0.8279. Nilai ini menunjukkan bahwa model memiliki kemampuan yang baik dalam membedakan antara pelanggan yang churn dan tidak churn. 

Nilai ROC-AUC berkisar antara 0 hingga 1, di mana nilai 0.5 menunjukkan model yang tidak lebih baik dari tebakan acak, sedangkan nilai 1 menunjukkan model yang sempurna. Dengan nilai 0.8279, model Logistic Regression ini menunjukkan performa yang cukup baik dalam memprediksi churn pelanggan.

\subsubsection{Interpretasi Hasil dan Implikasi Bisnis}
Hasil pengujian model Logistic Regression menunjukkan bahwa model ini memiliki akurasi sekitar 72.4\%, dengan presisi 48.8\%, recall 79.1\%, dan F1-score 60.4\%. Meskipun model ini mampu menangkap sebagian besar pelanggan yang churn (recall yang tinggi), namun presisi yang rendah menunjukkan bahwa model ini juga menghasilkan banyak false positive, yaitu pelanggan yang diprediksi churn padahal sebenarnya tidak yang mungkin akan mengakibatkan perusahaan mengeluarkan biaya tambahan untuk intervensi yang tidak perlu, seperti penawaran khusus atau peningkatan layanan kepada pelanggan yang sebenarnya tidak berisiko churn.

Menurut saya metriks evaluasi yang paling relevan untuk kasus ini adalah recall, karena dalam konteks bisnis, penting untuk menangkap sebanyak mungkin pelanggan yang berpotensi churn. Dengan recall yang tinggi, perusahaan dapat lebih proaktif dalam melakukan intervensi untuk mencegah churn, seperti memberikan penawaran khusus atau meningkatkan kualitas layanan.

Namun, perlu diingat bahwa model ini masih memiliki ruang untuk perbaikan, terutama dalam hal presisi. Perusahaan perlu mempertimbangkan untuk melakukan hyperparameter tuning atau mencoba model lain yang lebih kompleks untuk meningkatkan performa prediksi churn. Pada tahap selanjutnya, akan dilakukan hyperparameter tuning untuk mencoba meningkatkan presisi tanpa mengorbankan recall yang tinggi.

\subsubsection{Hasil Logistic Regression dengan hyperparameter tuning}

Berdasarkan hasil model baseline Logistic Regression, diketahui bahwa model telah memiliki nilai \textit{recall} yang cukup baik namun masih memiliki ruang perbaikan pada metrik \textit{precision}. Oleh karena itu, dilakukan proses optimisasi hyperparameter untuk mencari kombinasi parameter yang lebih optimal. Metode yang digunakan adalah \textit{RandomizedSearchCV} dengan skema \textit{Stratified K-Fold Cross-Validation} untuk memastikan proporsi kelas tetap seimbang pada setiap fold. Hyperparameter yang diujikan meliputi c, penalty, solver, dan class\_weight. Kriteria pemilihan model terbaik ditentukan berdasarkan metrik \textit{recall} untuk memaksimalkan kemampuan model dalam mendeteksi pelanggan yang berpotensi churn.

Pemilihan kombinasi terbaik dilakukan berdasarkan metrik \textit{recall}, mengingat tujuan utama dalam kasus ini adalah memaksimalkan deteksi pelanggan churn. Adapun ruang pencarian hyperparameter yang digunakan ditunjukkan pada Tabel \ref{tab:search_space_log_reg}.

\begin{table}[H]
    \centering
    \caption{Ruang Pencarian Hyperparameter Logistic Regression}
    \label{tab:search_space_log_reg}
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Hyperparameter} & \textbf{Nilai yang diuji} \\ \hline
        Solver & \texttt{liblinear}, \texttt{saga} \\ \hline
        Penalty & \texttt{l1}, \texttt{l2} \\ \hline
        Regularisasi $C$ & Distribusi log-uniform $[10^{-3}, 10^{2}]$ \\ \hline
        Class Weight & \texttt{None}, \texttt{balanced}, \{0:1, 1:2\}, \{0:1, 1:3\} \\ \hline
    \end{tabular}
\end{table}

Proses tuning dilakukan menggunakan $n\_iter = 50$, yang berarti \textit{RandomizedSearchCV} akan menguji 50 kombinasi hyperparameter secara acak dari ruang pencarian yang ada. Evaluasi dilakukan menggunakan skema \textit{Stratified K-Fold Cross-Validation} untuk mempertahankan proporsi kelas pada setiap fold. Skor yang dihitung meliputi \textit{recall}, \textit{precision}, \textit{f1-score}, dan \textit{ROC-AUC}, dengan metrik \textit{recall} sebagai acuan utama untuk pemilihan model terbaik.

Setelah dilakukan RandomizedSearchCV, diperoleh confusion matrix dari model Logistic Regression yang telah dioptimasi hyperparameternya, seperti pada Gambar \ref{fig:confusion_matrix_logistic_regression_tuned}. Confusion matrix ini menunjukkan performa model dalam mengklasifikasikan data uji setelah proses tuning.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{gambar/bestlog.png}
    \caption{Confusion Matrix Model Logistic Regression dengan Hyperparameter Tuning}
    \label{fig:confusion_matrix_logistic_regression_tuned}
\end{figure}

Dari gambar \ref{fig:confusion_matrix_logistic_regression_tuned}, kita dapat mengidentifikasi performa model Logistic Regression yang telah dioptimasi hyperparameter. Confusion matrix ini memberikan informasi tentang jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Detailnya adalah sebagai berikut:

\begin{itemize}
    \item True Negative (TN): 708
    \item False Positive (FP): 325
    \item False Negative (FN): 73
    \item True Positive (TP): 301
\end{itemize}

Setelah mendapatkan confusion matrix, kita dapat menghitung beberapa metrik evaluasi untuk model Logistic Regression yang telah dioptimasi hyperparameter. Metrik yang umum digunakan adalah akurasi, presisi, recall, dan F1-score.

\subsubsection*{Akurasi}
Akurasi adalah proporsi prediksi yang benar dari total prediksi yang dilakukan oleh model. Berikut adalah perhitungan akurasi model Logistic Regression setelah hyperparameter tuning:
\begin{equation}
    \text{Akurasi} = \frac{TP + TN}{TP + TN + FP + FN} = \frac{301 + 708}{301 + 708 + 325 + 73} = \frac{1009}{1407} \approx 0.717
\end{equation}
Akurasi model Logistic Regression setelah hyperparameter tuning adalah sekitar 71.7\%. Ini menunjukkan bahwa model ini mampu memprediksi dengan benar sekitar 71.7\% dari total data uji yang diberikan.
\subsubsection*{Precision}
Precision adalah proporsi prediksi positif yang benar dari total prediksi positif yang dilakukan oleh model. Berikut adalah perhitungan presisi model Logistic Regression setelah hyperparameter tuning:
\begin{equation}
    \text{Presisi} = \frac{TP}{TP + FP} = \frac{301}{301 + 325} = \frac{301}{626} \approx 0.481
\end{equation}
Presisi model Logistic Regression setelah hyperparameter tuning adalah sekitar 48.1\%. Ini menunjukkan bahwa dari semua pelanggan yang diprediksi churn, hanya sekitar 48.1\% yang benar-benar churn. Hal ini mengindikasikan bahwa model ini masih menghasilkan cukup banyak false positive, yaitu pelanggan yang diprediksi churn padahal sebenarnya tidak.
\subsubsection*{Recall}
Recall adalah proporsi prediksi positif yang benar dari total aktual positif. Berikut adalah perhitungan recall model Logistic Regression setelah hyperparameter tuning:
\begin{equation}
    \text{Recall} = \frac{TP}{TP + FN} = \frac{301}{301 + 73} = \frac{301}{374} \approx 0.804
\end{equation}
Recall model Logistic Regression setelah hyperparameter tuning adalah sekitar 80.4\%. Ini menunjukkan bahwa model ini mampu menangkap sekitar 80.4\% dari pelanggan yang sebenarnya churn, namun masih ada 19.6\% pelanggan churn yang tidak terdeteksi oleh model.
\subsubsection*{F1-score}
F1-score adalah rata-rata harmonis dari presisi dan recall. F1-score memberikan gambaran yang lebih baik tentang keseimbangan antara presisi dan recall. Berikut adalah perhitungan F1-score model Logistic Regression setelah hyperparameter tuning:
\begin{equation}
    \text{F1-score} = 2 \cdot \frac{\text{Presisi} \cdot \text{Recall}}{\text{Presisi} + \text{Recall}} = 2 \cdot \frac{0.481 \cdot 0.804}{0.481 + 0.804} \approx 0.604
\end{equation}
F1-score model Logistic Regression setelah hyperparameter tuning adalah sekitar 60.4\%. Ini menunjukkan bahwa model ini memiliki keseimbangan yang cukup baik antara presisi dan recall, meskipun masih ada ruang untuk perbaikan, terutama dalam hal menangkap pelanggan yang churn.

\subsubsection*{ROC-AUC}
ROC-AUC (Receiver Operating Characteristic - Area Under the Curve) adalah metrik yang digunakan untuk mengukur kemampuan model dalam membedakan antara kelas positif dan negatif. Adapun nilai ROC-AUC yang didapatkan dari model Logistic Regression setelah hyperparameter tuning adalah 0.8279. Nilai ini menunjukkan bahwa model memiliki kemampuan yang baik dalam membedakan antara pelanggan yang churn dan tidak churn.

Nilai ROC-AUC berkisar antara 0 hingga 1, di mana nilai 0.5 menunjukkan model yang tidak lebih baik dari tebakan acak, sedangkan nilai 1 menunjukkan model yang sempurna. Dengan nilai 0.8279, model Logistic Regression ini menunjukkan performa yang cukup baik dalam memprediksi churn pelanggan.

\subsubsection{Parameter Terbaik}

Nilai nilai diatas diperoleh dari proses \textit{RandomizedSearchCV} yang telah dilakukan sebelumnya. Adapun kombinasi hyperparameter untuk mendapatkan hasil diatas ditunjukkan pada Tabel \ref{tab:best_params_logreg}. Kombinasi ini merupakan hasil terbaik yang diperoleh dari proses tuning hyperparameter, yang bertujuan untuk meningkatkan performa model dalam mendeteksi pelanggan yang churn.

\begin{table}[H]
    \centering
    \caption{Kombinasi Hyperparameter Terbaik (RandomizedSearchCV)}
    \label{tab:best_params_logreg}
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Hyperparameter} & \textbf{Nilai Terbaik} \\ \hline
        $C$ & 40.679 \\ \hline
        \texttt{class\_weight} & \{0: 1,\; 1: 3\} \\ \hline
        \texttt{penalty} & \texttt{l2} \\ \hline
        \texttt{solver} & \texttt{liblinear} \\ \hline
    \end{tabular}
\end{table}

\subsubsection{Perbandingan Hasil Logistic Regression Sebelum dan Sesudah Hyperparameter Tuning}
Setelah melakukan hyperparameter tuning, kita dapat membandingkan hasil model Logistic Regression sebelum dan sesudah tuning. Tabel \ref{tab:comparison_logreg} menunjukkan perbandingan metrik evaluasi antara model baseline dan model yang telah dioptimasi hyperparameter.

\begin{table}[H]
    \centering
    \caption{Perbandingan Hasil Model Logistic Regression Sebelum dan Sesudah Hyperparameter Tuning}
    \label{tab:comparison_logreg}
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Metrik} & \textbf{Sebelum Tuning} & \textbf{Setelah Tuning} \\ \hline
        Akurasi & 72.4\% & 71.7\% \\ \hline
        Presisi & 48.8\% & 48.1\% \\ \hline
        Recall & 79.1\% & 80.4\% \\ \hline
        F1-score & 60.4\% & 60.4\% \\ \hline
        ROC-AUC & 0.8279 & 0.8279 \\ \hline
    \end{tabular}
\end{table}

Dari tabel \ref{tab:comparison_logreg}, dapat dilihat bahwa setelah melakukan hyperparameter tuning, model Logistic Regression mengalami peningkatan pada metrik recall, yaitu dari 79.1\% menjadi 80.4\%. Hal ini menunjukkan bahwa model yang telah dioptimasi lebih baik dalam mendeteksi pelanggan yang berpotensi churn.

Namun, akurasi dan presisi model mengalami penurunan, yaitu dari 72.4\% menjadi 71.7\% untuk akurasi dan dari 48.8\% menjadi 48.1\% untuk presisi. ROC-AUC tetap sama pada nilai 0.8279, yang menunjukkan bahwa tuning yang dilakukan fokus pada peningkatan recall tanpa mengorbankan kemampuan model dalam membedakan antara kelas churn dan tidak churn.

Meskipun demikian, peningkatan recall yang signifikan menunjukkan bahwa model ini lebih efektif dalam menangkap pelanggan churn, yang merupakan tujuan utama dari analisis ini.

\newpage
\subsection{Pengujian Model Decision Tree}
Pengujian model Decision Tree dilakukan dengan menggunakan data uji yang telah disiapkan. Model ini dilatih pada data latih dan kemudian diuji pada data uji untuk mengevaluasi performanya. Hasil dari pengujian ini akan memberikan gambaran tentang seberapa baik model dalam memprediksi churn pelanggan.

Setelah dilakukan pengujian model Decision Tree, diperoleh hasil confusion matrix yang merepresentasikan performa model dalam mengklasifikasikan data uji. Confusion matrix ini menunjukkan jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Dari confusion matrix, dapat dihitung beberapa metrik evaluasi seperti akurasi, presisi, recall, dan F1-score.

\subsubsection{Hasil Decision Tree tanpa hyperparameter tuning}
% tiru penjelasan logistic regression di atas namun sesuaikan dengan penjelasan subbab ini yaitu decision tree
Berikut adalah hasil confusion matrix dari model Decision Tree yang telah diuji pada data uji, Gambar \ref{fig:decision_tree} menunjukkan confusion matrix yang diperoleh dari model Decision Tree tanpa melakukan hyperparameter tuning. Sehingga baseline model Decision Tree ini akan menjadi acuan untuk perbandingan dengan model-model lainnya yang akan diuji pada tahap selanjutnya. Namun kita tetap perlu menambahkan beberapa setting manual karena pada bab sebelumnya kita menemukan imbalance data pada dataset yang digunakan. Oleh karena itu, kita perlu mengatur beberapa parameter pada model Decision Tree untuk mengatasi masalah tersebut.  Parameter yang digunakan adalah sebagai berikut:

\begin{itemize}
    \item \texttt{criterion="gini"}: Fungsi pengukuran impurity yang digunakan adalah Gini Impurity (default). Alternatif lainnya adalah \texttt{"entropy"} yang menggunakan Information Gain.
    \item \texttt{splitter="best"}: Strategi pemilihan split pada setiap node adalah memilih split terbaik (default). Alternatifnya adalah \texttt{"random"} untuk pemilihan acak.
    \item \texttt{max\_depth=None}: Kedalaman pohon tidak dibatasi (default), sehingga pohon akan tumbuh hingga semua daun bersifat \textit{pure} atau tidak dapat di-split lagi. Hal ini berpotensi menimbulkan \textit{overfitting}.
    \item \texttt{min\_samples\_split=2}: Minimal jumlah sampel untuk melakukan split pada sebuah node adalah 2 (default).
    \item \texttt{min\_samples\_leaf=1}: Minimal jumlah sampel pada sebuah daun adalah 1 (default).
    \item \texttt{max\_features=None}: Semua fitur digunakan untuk mencari split terbaik (default). 
    \item \texttt{class\_weight="balanced"}: Memberikan bobot kelas secara otomatis yang proporsional terhadap kebalikannya dari frekuensi kelas, untuk mengatasi ketidakseimbangan kelas pada dataset.
    \item \texttt{random\_state=42}: Menetapkan nilai \textit{seed} untuk memastikan hasil dapat direproduksi.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{gambar/decisiontreefix.png}
    \caption{Confusion Matrix Model Decision Tree}
    \label{fig:decision_tree}
\end{figure}
Dari gambar \ref{fig:decision_tree} kita bisa identifikasi performa model Decision Tree dalam mengklasifikasikan data uji. Confusion matrix ini memberikan informasi tentang jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Detailnya adalah sebagai berikut:
\begin{itemize}
    \item True Negative (TN): 799 (Pelanggan yang benar-benar tidak churn dan diprediksi tidak churn)
    \item False Positive (FP): 234 (Pelanggan yang tidak churn, tetapi diprediksi churn)
    \item False Negative (FN): 154 (Pelanggan yang churn, tetapi diprediksi tidak churn)
    \item True Positive (TP): 220 (Pelanggan yang benar-benar churn dan diprediksi churn)
\end{itemize}

Setelah mendapatkan confusion matrix, kita dapat menghitung beberapa metrik evaluasi untuk model Decision Tree. Metrik yang umum digunakan adalah akurasi, presisi, recall, dan F1-score.

\subsubsection*{Akurasi}
Akurasi adalah proporsi prediksi yang benar dari total prediksi yang dilakukan oleh model. Berikut adalah perhitungan akurasi model Decision Tree:

\begin{equation}
    \text{Akurasi} = \frac{TP + TN}{TP + TN + FP + FN} = \frac{220 + 799}{220 + 799 + 234 + 154} = \frac{1019}{1407} \approx 0.724
\end{equation}

Akurasi model Decision Tree adalah sekitar 72.4\%. Ini menunjukkan bahwa model ini mampu memprediksi dengan benar sekitar 72.4\% dari total data uji yang diberikan.

\subsubsection*{Precision}
Precision adalah proporsi prediksi positif yang benar dari total prediksi positif yang dilakukan oleh model. Berikut adalah perhitungan presisi model Decision Tree:

\begin{equation}
    \text{Presisi} = \frac{TP}{TP + FP} = \frac{220}{220 + 234} = \frac{220}{454} \approx 0.485
\end{equation}

Presisi model Decision Tree adalah sekitar 48.5\%. Ini menunjukkan bahwa dari semua pelanggan yang diprediksi churn, hanya sekitar 48.5\% yang benar-benar churn. Hal ini mengindikasikan bahwa model ini masih menghasilkan cukup banyak false positive, yaitu pelanggan yang diprediksi churn padahal sebenarnya tidak.

\subsubsection*{Recall}
Recall adalah proporsi prediksi positif yang benar dari total aktual positif. Berikut adalah perhitungan recall model Decision Tree:

\begin{equation}
    \text{Recall} = \frac{TP}{TP + FN} = \frac{220}{220 + 154} = \frac{220}{374} \approx 0.588
\end{equation}

Recall model Decision Tree adalah sekitar 58.8\%. Ini menunjukkan bahwa model ini mampu menangkap sekitar 58.8\% dari pelanggan yang sebenarnya churn, namun masih ada 41.2\% pelanggan churn yang tidak terdeteksi oleh model.

\subsubsection*{F1-score}
F1-score adalah rata-rata harmonis dari presisi dan recall. F1-score memberikan gambaran yang lebih baik tentang keseimbangan antara presisi dan recall. Berikut adalah perhitungan F1-score model Decision Tree:

\begin{equation}
    \text{F1-score} = 2 \cdot \frac{\text{Presisi} \cdot \text{Recall}}{\text{Presisi} + \text{Recall}} = 2 \cdot \frac{0.485 \cdot 0.588}{0.485 + 0.588} \approx 0.531
\end{equation}

F1-score model Decision Tree adalah sekitar 53.1\%. Ini menunjukkan bahwa model ini memiliki keseimbangan yang cukup baik antara presisi dan recall, meskipun masih ada ruang untuk perbaikan, terutama dalam hal menangkap pelanggan yang churn.

\subsubsection*{ROC-AUC}
ROC-AUC (Receiver Operating Characteristic - Area Under the Curve) adalah metrik yang digunakan untuk mengukur kemampuan model dalam membedakan antara kelas positif dan negatif. Adapun nilai ROC-AUC yang didapatkan dari model Decision Tree adalah 0.6866. Nilai ini menunjukkan bahwa model memiliki kemampuan yang cukup baik dalam membedakan antara pelanggan yang churn dan tidak churn.

Nilai ROC-AUC berkisar antara 0 hingga 1, di mana nilai 0.5 menunjukkan model yang tidak lebih baik dari tebakan acak, sedangkan nilai 1 menunjukkan model yang sempurna. Dengan nilai 0.6866, model Decision Tree ini menunjukkan performa yang cukup baik dalam memprediksi churn pelanggan.

\subsubsection{Hasil Decision Tree dengan hyperparameter tuning}
Setelah memperoleh hasil dari model Decision Tree tanpa hyperparameter tuning, langkah selanjutnya adalah melakukan \textit{hyperparameter tuning} untuk meningkatkan performa model, khususnya dalam hal \textit{recall} yang menjadi fokus utama pada kasus prediksi \textit{churn}. Proses \textit{tuning} dilakukan menggunakan \texttt{RandomizedSearchCV} dengan skema \textit{cross-validation}, di mana metrik utama yang digunakan sebagai acuan pemilihan model terbaik adalah \textit{recall}.

Ruang pencarian (\textit{search space}) untuk parameter model Decision Tree dirancang dengan mempertimbangkan keseimbangan antara kompleksitas model dan risiko \textit{overfitting}, yaitu sebagai berikut:

% \begin{itemize}
%     \item \texttt{criterion}: \{\texttt{"gini"}, \texttt{"entropy"}, \texttt{"log\_loss"}\} – Fungsi pengukuran impurity untuk menentukan kualitas \textit{split}.
%     \item \texttt{max\_depth}: \{3, 4, ..., 30\} – Kedalaman maksimum pohon; semakin besar nilai ini, model cenderung lebih kompleks.
%     \item \texttt{min\_samples\_split}: \{2, 3, ..., 50\} – Jumlah minimum sampel untuk memecah sebuah node.
%     \item \texttt{min\_samples\_leaf}: \{1, 2, ..., 30\} – Jumlah minimum sampel pada sebuah daun.
%     \item \texttt{max\_features}: \{\texttt{None}, \texttt{"sqrt"}, \texttt{"log2"}\} – Jumlah fitur yang dipertimbangkan pada setiap \textit{split}.
%     \item \texttt{class\_weight}: \{\texttt{None}, \texttt{"balanced"}\} – Penyesuaian bobot kelas untuk menangani ketidakseimbangan data.
%     \item \texttt{ccp\_alpha}: [0.0, 0.02] – Parameter \textit{cost-complexity pruning} untuk memangkas cabang kecil.
%     \item \texttt{splitter}: \{\texttt{"best"}\} – Strategi pemilihan \textit{split} terbaik pada setiap node.
% \end{itemize}
%buat tabel ruang pencarian
\begin{table}[H]
    \centering
    \caption{Ruang Pencarian Hyperparameter Decision Tree}
    \label{tab:search_space_decision_tree}
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Hyperparameter} & \textbf{Nilai yang diuji} \\ \hline
        Criterion & \texttt{"gini"}, \texttt{"entropy"} \\ \hline
        Max Depth & 3, 4, ..., 30 \\ \hline
        Min Samples Split & 2, 3, ..., 50 \\ \hline
        Min Samples Leaf & 1, 2, ..., 30 \\ \hline
        Max Features & \texttt{None}, \texttt{"sqrt"}, \texttt{"log2"} \\ \hline
        Class Weight & \texttt{None}, \texttt{"balanced"} \\ \hline
        Ccp Alpha & [0.0, 0.02] \\ \hline
        Splitter & \texttt{"best"} \\ \hline
    \end{tabular}
\end{table}

Dari tabel diatas 80 iterasi \texttt{RandomizedSearchCV} dilakukan untuk mencari kombinasi hyperparameter yang optimal. Proses ini bertujuan untuk menemukan konfigurasi yang memberikan nilai \textit{recall} tertinggi, sehingga model dapat lebih efektif dala   m mendeteksi pelanggan yang berpotensi churn.

Setelah dilakukan \texttt{RandomizedSearchCV}, diperoleh confusion matrix dari model Decision Tree yang telah dioptimasi hyperparameternya, seperti pada Gambar \ref{fig:decision_tree_tuned}. Confusion matrix ini menunjukkan performa model dalam mengklasifikasikan data uji setelah proses tuning.

\newpage
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{gambar/bestdt.png}
    \caption{Confusion Matrix Model Decision Tree dengan Hyperparameter Tuning}
    \label{fig:decision_tree_tuned}
\end{figure}
Dari gambar \ref{fig:decision_tree_tuned}, kita dapat mengidentifikasi performa model Decision Tree yang telah dioptimasi hyperparameter. Confusion matrix ini memberikan informasi tentang jumlah prediksi yang benar dan salah untuk masing-masing kelas (churn dan tidak churn). Detailnya adalah sebagai berikut:

\begin{itemize}
    \item True Negative (TN): 569 (Pelanggan yang benar-benar tidak churn dan diprediksi tidak churn)
    \item False Positive (FP): 464 (Pelanggan yang tidak churn, tetapi diprediksi churn)
    \item False Negative (FN): 48 (Pelanggan yang churn, tetapi diprediksi tidak churn)
    \item True Positive (TP): 326 (Pelanggan yang benar-benar churn dan diprediksi churn)
\end{itemize}

Setelah mendapatkan confusion matrix, kita dapat menghitung beberapa metrik evaluasi untuk model Decision Tree yang telah dioptimasi hyperparameter. Metrik yang umum digunakan adalah akurasi, presisi, recall, dan F1-score.

\subsubsection*{Akurasi}
Akurasi adalah proporsi prediksi yang benar dari total prediksi yang dilakukan oleh model. Berikut adalah perhitungan akurasi model Decision Tree setelah hyperparameter tuning:
\begin{equation}
    \text{Akurasi} = \frac{TP + TN}{TP + TN + FP + FN} = \frac{326 + 569}{326 + 569 + 464 + 48} = \frac{895}{1407} \approx 0.636
\end{equation}

Akurasi model Decision Tree setelah hyperparameter tuning adalah sekitar 63.6\%. Ini menunjukkan bahwa model ini mampu memprediksi dengan benar sekitar 63.6\% dari total data uji yang diberikan.

\subsubsection*{Precision}
Precision adalah proporsi prediksi positif yang benar dari total prediksi positif yang dilakukan oleh model. Berikut adalah perhitungan presisi model Decision Tree setelah hyperparameter tuning:

\begin{equation}
    \text{Presisi} = \frac{TP}{TP + FP} = \frac{326}{326 + 464} = \frac{326}{790} \approx 0.413
\end{equation}

Presisi model Decision Tree setelah hyperparameter tuning adalah sekitar 41.3\%. Ini menunjukkan bahwa dari semua pelanggan yang diprediksi churn, hanya sekitar 41.3\% yang benar-benar churn. Hal ini mengindikasikan bahwa model ini masih menghasilkan cukup banyak false positive, yaitu pelanggan yang diprediksi churn padahal sebenarnya tidak.

\subsubsection*{Recall}
Recall adalah proporsi prediksi positif yang benar dari total aktual positif. Berikut adalah perhitungan recall model Decision Tree setelah hyperparameter tuning:

\begin{equation}
    \text{Recall} = \frac{TP}{TP + FN} = \frac{326}{326 + 48} = \frac{326}{374} \approx 0.872
\end{equation}

Recall model Decision Tree setelah hyperparameter tuning adalah sekitar 87.2\%. Ini menunjukkan bahwa model ini mampu menangkap sekitar 87.2\% dari pelanggan yang sebenarnya churn, namun masih ada 12.8\% pelanggan churn yang tidak terdeteksi oleh model.

\subsubsection*{F1-score}
F1-score adalah rata-rata harmonis dari presisi dan recall. F1-score memberikan gambaran yang lebih baik tentang keseimbangan antara presisi dan recall. Berikut adalah perhitungan F1-score model Decision Tree setelah hyperparameter tuning:

\begin{equation}
    \text{F1-score} = 2 \cdot \frac{\text{Presisi} \cdot \text{Recall}}{\text{Presisi} + \text{Recall}} = 2 \cdot \frac{0.413 \cdot 0.872}{0.413 + 0.872} \approx 0.561
\end{equation}

F1-score model Decision Tree setelah hyperparameter tuning adalah sekitar 56.1\%. Ini menunjukkan bahwa model ini memiliki keseimbangan yang cukup baik antara presisi dan recall, meskipun masih ada ruang untuk perbaikan, terutama dalam hal menangkap pelanggan yang churn.

\subsubsection*{ROC-AUC}
ROC-AUC (Receiver Operating Characteristic - Area Under the Curve) adalah metrik yang digunakan untuk mengukur kemampuan model dalam membedakan antara kelas positif dan negatif. Adapun nilai ROC-AUC yang didapatkan dari model Decision Tree setelah hyperparameter tuning adalah 0.7880. Nilai ini menunjukkan bahwa model memiliki kemampuan yang cukup baik dalam membedakan antara pelanggan yang churn dan tidak churn.

Nilai ROC-AUC berkisar antara 0 hingga 1, di mana nilai 0.5 menunjukkan model yang tidak lebih baik dari tebakan acak, sedangkan nilai 1 menunjukkan model yang sempurna. Dengan nilai 0.7880, model Decision Tree ini menunjukkan performa yang cukup baik dalam memprediksi churn pelanggan.

\subsubsection{Parameter Terbaik}
Nilai-nilai di atas diperoleh dari proses \texttt{RandomizedSearchCV} yang telah dilakukan sebelumnya. Adapun kombinasi hyperparameter untuk mendapatkan hasil di atas ditunjukkan pada Tabel \ref{tab:best_params_decision_tree}. Kombinasi ini merupakan hasil terbaik yang diperoleh dari proses tuning hyperparameter, yang bertujuan untuk meningkatkan performa model dalam mendeteksi pelanggan yang churn.

% \begin{verbatim}
%     criterion     : entropy
%     max_depth     : 10
%     min_samples_split : 5
%     min_samples_leaf  : 2
%     max_features  : sqrt
%     class_weight  : balanced
%     ccp_alpha     : 0.005
%     splitter      : best
%     \end{verbatim}
%buatkan tabel untuk kombinasi hyperparameter terbaik

\begin{table}[H]
    \centering
    \caption{Kombinasi Hyperparameter Terbaik (RandomizedSearchCV)}
    \label{tab:best_params_decision_tree}
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Hyperparameter} & \textbf{Nilai Terbaik} \\ \hline
        Criterion & log\_loss \\ \hline
        Max Depth & 22 \\ \hline
        Min Samples Split & 38 \\ \hline
        Min Samples Leaf & 3 \\ \hline
        Max Features & None \\ \hline
        Class Weight & balanced \\ \hline
        Ccp Alpha & 0.0123677 \\ \hline
        Splitter & best \\ \hline
    \end{tabular}
\end{table}

\subsubsection{Perbandingan Hasil Decision Tree Sebelum dan Sesudah Hyperparameter Tuning}
Setelah melakukan hyperparameter tuning, kita dapat membandingkan hasil model Decision Tree sebelum dan sesudah tuning. Tabel \ref{tab:comparison_decision_tree} menunjukkan perbandingan metrik evaluasi antara model baseline dan model yang telah dioptimasi hyperparameter.
\begin{table}[H]
    \centering
    \caption{Perbandingan Hasil Model Decision Tree Sebelum dan Sesudah Hyperparameter Tuning}
    \label{tab:comparison_decision_tree}
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Metrik} & \textbf{Sebelum Tuning} & \textbf{Setelah Tuning} \\ \hline
        Akurasi & 72.4\% & 63.6\% \\ \hline
        Presisi & 48.5\% & 41.3\% \\ \hline
        Recall & 58.8\% & 87.2\% \\ \hline
        F1-score & 53.1\% & 56.1\% \\ \hline
        ROC-AUC & 0.6866 & 0.7880 \\ \hline
    \end{tabular}
\end{table}
Dari tabel \ref{tab:comparison_decision_tree}, dapat dilihat bahwa setelah melakukan hyperparameter tuning, model Decision Tree mengalami peningkatan yang signifikan pada metrik recall, yaitu dari 58.8\% menjadi 87.2\%. Hal ini menunjukkan bahwa model yang telah dioptimasi lebih baik dalam mendeteksi pelanggan yang berpotensi churn.

Namun, akurasi dan presisi model mengalami penurunan, yaitu dari 72.4\% menjadi 63.6\% untuk akurasi dan dari 48.5\% menjadi 41.3\% untuk presisi. Meskipun demikian, peningkatan recall yang signifikan menunjukkan bahwa model ini lebih efektif dalam menangkap pelanggan churn, yang merupakan tujuan utama dari analisis ini.

Meskipun akurasi menurun, peningkatan recall menunjukkan bahwa model ini lebih fokus pada deteksi pelanggan churn, yang merupakan prioritas utama dalam konteks ini. Nilai ROC-AUC juga meningkat dari 0.6866 menjadi 0.7880, menunjukkan bahwa model Decision Tree yang telah dioptimasi lebih baik dalam membedakan antara pelanggan yang churn dan tidak churn.


    















